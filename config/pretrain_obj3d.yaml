# SlotPi 预训练配置 — OBJ3D（仅 STATM-SAVi，无 rollout）
dataset: "obj3d"
data_root: "./data/obj3d"
num_workers: 4

pretrain: true

# 编码器（OBJ3D 分辨率 64×64）
encoder_type: "cnn"
in_channels: 3
img_size: 64
encoder_hidden: 64

# Slot Attention
num_slots: 7
static_dim: 96
dynamic_dim: 32
slot_hidden: 128

# 解码器
decoder_hidden: [64, 64, 64, 64]
broadcast_size: [8, 8]

# 物理模块（预训练不启用，保留默认值）
hidden_dim: 256
num_heads: 4
qkv_size: 128
mlp_size: 256
num_layers: 2
h_layers: 1
out_mlp: false
out_hidden_layers: 1
pre_norm: false
dropout_rate: 0.1
delta_t: 0.08
integrator_method: "Euler"
num_spatiotemporal_blocks: 2

# 时域降采样（原始 25fps → 12.5fps）
subsample: 2

# 训练
num_steps: 100000
batch_size: 64
learning_rate: 0.0002
weight_decay: 0.0
max_grad_norm: 0.05
burnin_frames: 6
rollout_frames: 0
slide_stride: 4
warmup_steps: 2500

# 损失权重（预训练仅重建损失）
lambda_slots: 0.0
lambda_images: 0.0
lambda_energy: 0.0
lambda_static: 0.0
lambda_rev: 0.0
lambda_recon_burnin: 1.0
lambda_recon_rollout: 0.0
gamma_max: 0.0
c_gamma: 10.0
rev_mlp_hidden: 128

rev_grad_max_norm: 0.05

# 冻结静态特征
freeze_C: false

log_every: 10
eval_every_epochs: 4
save_every: 1000
keep_last_checkpoints: 10
workdir: "./pretrained/obj3d"

# 断点续训
resume: true
resume_step: 10000